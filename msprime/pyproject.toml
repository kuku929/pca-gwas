[project]
name = "msprime-sims"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pip",
    "msprime",
    "numpy",
    "pandas",
    "scipy",
    "plotly",
    "kaleido>=1.3.0",
]
